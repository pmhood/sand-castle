# Build context is images/agent. Keep credentials and editor/OS cruft out of every layer.
.env
*.env
.DS_Store
README.md

# Development-only: the checks that run around the image, never inside it.
.bats/
.gitignore
Makefile
bootstrap/test/
